const initialState = {
    users: [],
    students: [],
    number: 0,
    isLoggedIn: false,
};

const reducer = (state = initialState, action) => {
    switch (action.type) {
        // case "ADD_USER":
        
        // case "EDIT_USER":
           
        // case "DELETE_USER":
        
            default: 

            return state;
    }
    
};

export default reducer;